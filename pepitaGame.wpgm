import pepita.*
import comidas.*
import niveles.*
import extras.*
import wollok.game.*

program PepitaGame {
	game.title("Pepita")
	game.height(10)
	game.width(10)
	game.boardGround("fondo2.jpg")
	game.schedule(3000, { game.say(pepita, "¡Hola!") })
	keyboard.enter().onPressDo{game.say(pepita, pepita.energia().toString())}
	tutorial2.iniciar()
	
	game.showAttributes(pepita) //Debug
	
	game.start()

}